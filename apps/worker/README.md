# GrowLab Worker

.NET 10 Worker scaffold for:

- MQTT subscriptions
- telemetry processing
- heartbeat monitoring
- OTA job processing
- Shelly lighting sync
- alert processing

The MQTT client and database persistence are explicit placeholders in this first scaffold. Unsafe automation flags are checked and should remain disabled in the MVP.
